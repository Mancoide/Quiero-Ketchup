services:
    app:
        build:
            args:
                user: "${DOCKER_USER}"
                uid: "${DOCKER_UID}"
                ugroup: "${DOCKER_GROUP}"
                gid: "${DOCKER_GID}"
            context: ./
            dockerfile: Dockerfile
        image: "quiero-ketchup"
        container_name: "quiero-ketchup"
        hostname: "quiero-ketchup"
        restart: unless-stopped
        working_dir: /var/www/
        depends_on:
            - redis
            - reverb
        volumes:
            - ./:/var/www
            - ./mnt/exe/Imagenes:/var/www/storage/images
            - ./docker-compose/php/custom.ini:/usr/local/etc/php/conf.d/custom.ini
        networks:
            - "ketchup-net"
        environment:
            CONTAINER_ROLE: "${CONTAINER_ROLE}"
            DOCKER_USER: "${DOCKER_USER}"

    scheduler:
        image: "quiero-ketchup"
        container_name: "quiero-ketchup-scheduler"
        hostname: "quiero-ketchup-scheduler"
        depends_on:
            - app
        volumes:
            - .:/var/www
        networks:
            - "ketchup-net"
        environment:
            CONTAINER_ROLE: scheduler
            DOCKER_USER: "${DOCKER_USER}"

    queue:
        image: "quiero-ketchup"
        container_name: "quiero-ketchup-queue"
        hostname: "quiero-ketchup-queue"
        depends_on:
            - app
            - redis
        volumes:
            - ./:/var/www
        networks:
            - "ketchup-net"
        environment:
            CONTAINER_ROLE: queue
            DOCKER_USER: "${DOCKER_USER}"

    db:
        image: postgres
        restart: always
        container_name: "quiero-ketchup-pgdb"
        hostname: "quiero-ketchup-pgdb"
        environment:
            POSTGRES_PASSWORD: "${DB_PASSWORD}"
            POSTGRES_USER: "${DB_USERNAME}"
            POSTGRES_DB: "${DB_DATABASE}"
            PGDATA: /var/lib/postgresql/data/pgdata
        volumes:
            - ./pgdata:/var/lib/postgresql/data/pgdata
        ports:
            - "${DOCKER_HOST_DB_PORT}:${DOCKER_CONTAINER_DB_PORT}"
        networks:
            - "ketchup-net"

    nginx:
        image: nginx:alpine
        container_name: "quiero-ketchup-nginx"
        hostname: "quiero-ketchup-nginx"
        restart: unless-stopped
        depends_on:
            - app
        ports:
            - "${DOCKER_HOST_PORT}:${DOCKER_CONTAINER_PORT}"
        volumes:
            - ./:/var/www
            - ./docker-compose/nginx/nginx.conf:/etc/nginx/nginx.conf
            - ./docker-compose/nginx/conf.d:/etc/nginx/conf.d
        networks:
            - "ketchup-net"

    redis:
        container_name: "quiero-ketchup-redis"
        hostname: "quiero-ketchup-redis"
        image: redis:alpine
        ports:
            - "${DOCKER_HOST_REDIS_PORT}:${DOCKER_CONTAINER_REDIS_PORT}"
        volumes:
            - redis:/data
        networks:
            - "ketchup-net"
    reverb:
        image: php:8.3-cli
        container_name: laravel_reverb
        working_dir: /var/www/
        volumes:
          - .:/var/www/
        command: php artisan reverb:start --host=0.0.0.0 --port=8080
        ports:
          - "8080:8080"
        depends_on:
          - app
        networks:
          - "ketchup-net"

volumes:
    redis:
        driver: "local"

networks:
    ketchup-net:
        driver: bridge
